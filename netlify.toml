[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Static export already emits per-route HTML; this is a safety net for
# direct deep-link refreshes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
